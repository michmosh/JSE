drop down component with flags made with css and ul li elements
passes selected country as @Output() to parent component 
example : 
`<app-dropdown (selectedCountry)="selectCountry($event)" ></app-dropdown>`


